import React, { useState } from "react";

export interface SplitImageProps {
    before: string;
    after: string;
}

export function SplitImage(props: SplitImageProps) {
    const [splitWidth, setSplitWidth] = useState(.5);

    return (<div>
        
    </div>)
}